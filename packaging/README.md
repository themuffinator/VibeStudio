# Packaging

This directory follows the shape used by PakFu and will hold installer,
desktop integration, icon, MIME, and platform bundle metadata as VibeStudio
matures.

The initial scaffold keeps packaging metadata out of the build until product
icons, file associations, and release artifact names are finalized.
